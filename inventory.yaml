test:
  hosts:
    localhost:
      ansible_connection: local

whoami:
  hosts:
    localhost:
      ansible_connection: local

nginx:
  hosts:
    localhost:
      ansible_connection: local

apache:
  hosts:
    localhost:
      ansible_connection: local

