test:
  hosts:
    localhost:
      ansible_connection: local

groupe1:
  hosts:
    localhost:
      ansible_connection: local

groupe2:
  hosts:
    localhost:
      ansible_connection: local

groupe3:
  hosts:
    localhost:
      ansible_connection: local